{
  "name": "trtl-apps",
  "version": "0.2.9",
  "description": "a node-js package for interacting with turtle apps",
  "main": "dist/lib/index.js",
  "types": "dist/lib/index.d.ts",
  "scripts": {
    "build": "tsc",
    "docs": "./node_modules/.bin/typedoc ./lib && copyfiles --all --soft --up 1 docs-includes/* docs",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "files": [
    "dist/lib/*"
  ],
  "author": "zoidbergZA",
  "license": "GPL-3.0-or-later",
  "repository": {
    "type": "git",
    "url": "git://github.com/zoidbergZA/turtle-apps-node.git"
  },
  "dependencies": {
    "axios": "^0.19.0"
  },
  "devDependencies": {
    "typedoc": "^0.15.0"
  }
}
